<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- =============================== -->
    <!-- GROUPS -->
    <!-- =============================== -->

    <record id="group_purchase_own_documents" model="res.groups">
        <field name="name">Purchase: Own Documents</field>
    </record>

    <record id="group_purchase_all_documents" model="res.groups">
        <field name="name">Purchase: All Documents</field>
        <field name="implied_ids" eval="[(4, ref('purchase_security_structure.group_purchase_own_documents'))]"/>
    </record>


    <!-- =============================== -->
    <!-- RECORD RULES -->
    <!-- =============================== -->

    <!-- Own Documents -->
    <record id="purchase_order_rule_own_documents" model="ir.rule">
        <field name="name">Purchase Order: Own Documents Only</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('purchase_security_structure.group_purchase_own_documents'))]"/>
    </record>

    <!-- All Documents -->
    <record id="purchase_order_rule_all_documents" model="ir.rule">
        <field name="name">Purchase Order: All Documents</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4, ref('purchase_security_structure.group_purchase_all_documents'))]"/>
    </record>

</odoo>